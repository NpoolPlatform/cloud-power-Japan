export function updateVerifyCode(state, val) {
  state.verifyCode = val;
}

export function updateOldVerifyCode(state, val) {
  state.oldVerifyCode = val;
}

export function updateLoginVerify(state, val) {
  state.loginVerify = val;
}

export function updateRefresh(state, val) {
  state.refresh = val;
}
